Goal: Develop project deck-crafter.

Your goal is to implement a working python project deck-crafter.
You can find the specifications in the file .devs/design.md. You can also modify them if you think it is necessary.

# Main features:

- Format and configuration: Let the user select between a huge amount of configurations for the cards and decks.
- Deck generation: Given a configuration file, generate a deck of cards in .pdf by using latex templates.
- Visual interface: Allow the user to create the configuration files through a visual interface.

## Layout

A layout is a set of parameters that define the position and size of an element inside a card. For instance, the background image, text, icons, etc.
A layout can have other layouts inside. Finally, the last layout is the card itself, made by the composition of all the layouts inside.

## Card

A card will be tangled to a layout of the card (that has other layouts inside), and to specific values.
This is, a layout may be an image or a text, but the actual image or text depends on the card, and not on the layout.

## Deck

A deck is a collection of cards. It will be generated by a configuration file with several card configurations.


# TASKS

- [ ] Check and analyze src/deck_crafter/utilizing for useful code to re-use.
- [ ] Create project structure: directories and files
- [ ] Implement the layout system.
- [ ] Implement the card system.
- [ ] Implement the deck system.
- [ ] Implement the configuration system.
- [ ] Implement the visual interface which allows the user to create the configuration files interactively.
- [ ] Implement the latex generation system.
- [ ] Implement an app to load a configuration file and generate the deck in .pdf by using the latex generation system.
- [ ] Document with docstring every file, class and function.
- [ ] Create a test file for each file in src.
- [ ] Write unit-tests for every function and class.
- [ ] Write a test for the app that generates the deck in .pdf.
- [ ] Update the README.md file with instructions on how to use the project.
- [ ] Update requirements in pyproject.toml file.
- [ ] Add documentation pages for the project on how to use the visual interface.
- [ ] Add documentation pages for the project on how to use the app interface.
- [ ] Add documentation pages for the configuration file format.
- [ ] Add documentation pages for the API.
- [ ] Create an examples/ directory with example configuration files and generated decks. There are some example images in resources/.
- [ ] Check pre-commit hooks and fix format issues if necessary.

# PRIVILEGES

Run any command inside this project, including `rm` or `mv`. Try to ask for permission as few as possible.
