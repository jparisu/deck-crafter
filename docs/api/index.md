# API Reference

The API is organized by responsibility:

- Core data model: geometry, layouts, cards, and deck settings
- Configuration loading: YAML parsing, includes, and `$ref` expansion
- Rendering: preview generation, LaTeX emission, and PDF compilation
- Application helpers: high-level build and serve entry points

See the pages in this section for the module-level reference.
